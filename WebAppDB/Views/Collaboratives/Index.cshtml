@model IEnumerable<WebAppDB.Models.Collaborative>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Deadline)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaxNumParticipants)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quest)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Deadline)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaxNumParticipants)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quest.QuestId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QuestId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QuestId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QuestId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<div style="position: fixed; bottom: 10px; right: 10px;">
    <a asp-action="BackButton">Back to page</a>
</div>